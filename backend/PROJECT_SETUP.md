# Project Setup

First copy the .env file into the backend folder
Then, follow the docker commands

## Docker commands

``` sh
    docker compose up -d #for running in background
    docker compose up # To run in the current terminal
```